/** Isolated deterministic Harvester multiplayer state (client discovery FSM, protocol payloads, server-side rate limiting/player state, server identity hashing). No Minecraft/StationAPI/Fabric import. */
package io.github.sfaguiar.harvester.multiplayer;
